# 2026-09-15

## 4Geeks/BreatheCode token authentication
- Joe requested the first focused skill to validate `TOKEN_4GEEKS` and determine whether authentication is active.
- The API reference has no separate session-status endpoint. Validation uses one authenticated read-only `GET /v1/admissions/user/me`; HTTP 200 confirms current token acceptance and endpoint reachability, not a distinct server-side session.
- Applied Skill Workshop proposal: `breathecode-token-auth-check-20260915-a582c60c7b` (`breathecode-token-auth-check`).
- Tested the installed skill against `~/.openclaw/.env`: HTTP 200. The response body and token were not printed or logged; no write operation was performed.
- Full conversation and skill details recorded in `/root/.openclaw/workspace/SKILL_LOG.md`.

## Project status read skill
- Joe clarified that project retrieval must include current status: pending, submitted, or graded.
- Applied and tested `breathecode-active-projects-read-20260915-0a3b7a01c7` (`breathecode-active-projects-read`) v2.
- It uses one read-only `GET /v1/assignment/user/me/task` action filtered to `task_type=PROJECT`, with pagination and status mapping: `PENDING` → Pending, `DONE` → Submitted, `APPROVED` → Graded — approved, `REJECTED` → Graded — changes requested.
- Test result: HTTP 200; 62 project tasks returned — 12 PENDING and 50 DONE. No token or raw response data was exposed; no write operation was performed. Full details are in `SKILL_LOG.md`.

## Pending work read skill
- Joe requested a separate skill to list what remains to be completed.
- Applied and tested `breathecode-pending-work-read-20260915-984340c3df` (`breathecode-pending-work-read`).
- It uses one read-only `GET /v1/assignment/user/me/task` action filtered to `task_status=PENDING`, includes all task types, and supports pagination.
- Test result: HTTP 200; 43 pending tasks — 29 exercises, 2 lessons, and 12 projects. No token or raw response data was exposed; no write operation was performed. Full details are in `SKILL_LOG.md`.

## Progress summary read skill
- Joe requested a general overview of how far along he is in the course.
- Applied and tested `breathecode-progress-summary-read-20260915-cb0e269f02` (`breathecode-progress-summary-read`).
- It uses one read-only `GET /v1/activity/me` action for an activity-based summary; it must not invent a completion percentage unless the API supplies the necessary data.
- Test result: HTTP 403 from `/v1/activity/me`; no summary was retrieved. Token and response body were not exposed; no write operation was performed. Full details are in `SKILL_LOG.md`.

## Cohort enrollment read skill
- Joe requested a skill answering which cohort he is in, schedule, role, and `ACTIVE` versus `GRADUATED` status.
- Applied and tested `breathecode-my-cohorts-read-20260915-a5198a6664` (`breathecode-my-cohorts-read`).
- It uses one read-only `GET /v1/admissions/academy/cohort/me` action and reports all returned enrollments without inferring a single active cohort.
- Test result: HTTP 403 from `/v1/admissions/academy/cohort/me`; no cohort data was retrieved. Token and response body were not exposed; no write operation was performed. Full details are in `SKILL_LOG.md`.

## Task detail and feedback read skill
- Joe requested the final skill to retrieve one task’s details and feedback.
- Proposed `breathecode-task-detail-read-20260915-2bf2242528` (`breathecode-task-detail-read`), pending and not installed.
- It uses one read-only `GET /v1/assignment/task/{task_id}` action for one numeric task ID and returns detail, status, score, feedback, and review metadata when available.
- Raw task details and feedback must not be written to memory or logs. No API request or token access occurred. Full details are in `SKILL_LOG.md`.

Memory search was unavailable in this session because the memory index metadata is missing; reindex with `openclaw memory status --index` or `openclaw memory index --force` when appropriate.
